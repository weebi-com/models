
// import 'package:sembast/sembast.dart';

// extension SembastStoreExtension on StoreRef {
//   Future<bool> isEmpty(Database db) async =>
//     await this.count(db) == null || await this.count(db) == 0;

//   Future<bool> isNotEmpty(Database db) async => !await this.isEmpty(db);
// } 
